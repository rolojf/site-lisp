;;; setup-gren.el --- Insert description here -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:
;; (use-package gren-mode
;;   :straight (:type git :host github :repo "MaeBrooks/gren-mode" :branch "main")
;;   ;; :bind (:map outli-mode-map ; convenience key to get back to containing heading
;;   ;;             ("C-c C-p" . (lambda () (interactive) (outline-back-to-heading))))
;;   ;; :custom ()
;;   )
;;   (provide 'setup-gren)
;;; setup-gren.el ends here
